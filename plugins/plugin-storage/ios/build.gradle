dependencies {
    compile project(':core/ios')
    compile project(':plugin-storage')
}

ext.description = 'API to access storage features for the iOS platform'

def service = "Storage"

task xcodebuild {
    doLast {
        xcodebuildIOS("$project.buildDir","$project.projectDir", "$service")
    }
}

jar {
    dependsOn xcodebuild
    from("$project.buildDir/native/lib${service}.a")
    {
        into "native"
    }
}